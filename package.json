{
  "name": "machineoutcome-reference",
  "version": "1.0.0",
  "private": true,
  "description": "Public executable reference for fail-closed AI-agent mutation outcome verification.",
  "type": "module",
  "scripts": { "test": "node --test", "verify": "node --test" },
  "engines": { "node": ">=20" }
}
